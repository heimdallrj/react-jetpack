import React from 'react';
export declare const Tab: ({ title, selectedTab, onClick }: any) => JSX.Element;
export type TabsProps = {
    children: React.ReactNode | any;
};
export declare const Tabs: ({ children }: TabsProps) => JSX.Element;
export default Tabs;
