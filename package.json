{
  "private": true,
  "name": "jetpack-mono",
  "scripts": {
    "build": "cd ./jetpack/react && yarn build",
    "postbuild": "node ./scripts/build.js"
  },
  "workspaces": [
    "jetpack/*",
    "dist/*"
  ]
}
